# AI 眼镜测试工作台使用说明

这是第一版可运行框架，提供中文 Windows 窗口、Excel 模块选择、可更换设备、pytest 执行、SQLite 记录和独立结果文件。被测 APP 优先使用英文界面。已接入 G01_配对 的 G1/G3/G6 三条业务用例，G1 已实机验证。详见 docs/G系列配对自动化.md。

## 启动

双击根目录中的 **启动测试工作台.cmd**，或者运行 `bin/GlassesStudio/GlassesStudio.exe`。请保留整个目录，不要单独移动 EXE；`configs`、`cases` 和 `studio` 是配套配置及源代码。

已提供打包程序，无须另外安装 Python 才能打开窗口。设备操作仍需要 ADB；APP UI 自动化需要本机 Appium 和 UiAutomator2 驱动。当前电脑已有这些工具。

开发时也可以在此目录运行 `python launch.py`。pytest 依赖已随 `vendor` 提供；源码方式使用 Appium 还需要 `requirements.txt` 中的 Python 客户端。

## 每轮操作

1. 连接手机与眼镜，并在设备端完成 USB 调试授权。
2. 点击“选择 Excel”，选择 `.xlsx` 文件。编号列默认 A，结果列默认 L，表头行 0 表示各 Sheet 自动识别。修改映射后点击“重新读取”。
3. 模块列表展示文件中的全部 Sheet。点击首列勾选模块，或使用全选、取消全选。汇总页、空表、编号异常模块会显示但禁止执行；双击模块查看问题。
4. 点击“刷新设备”，分别选择手机与眼镜。列表显示系统型号、序列号和授权状态；眼镜未接 USB 时可选择“仅蓝牙连接（眼镜未接 USB）”。仅支持该模式的用例可执行，其他用例阻塞。
5. 填写眼镜蓝牙完整名称。G系列会从名称自动识别 G1/G3/G6，无须手动选择型号；三款共用功能，仅配对入口和显示有差异。其他产品仍可手动选择产品和型号。
6. 点击“连接检查”。需要执行 UI 用例时，再点击“启动 / 检查 Appium”。
7. 点击“开始执行”。只执行选中 Sheet 对应的已注册用例。窗口显示进度和各模块状态。
8. 如需停止，点击“停止执行”，等待当前用例停止并完成结果保存，再关闭窗口。
9. 点击“打开结果目录”，查看本轮 `report.html` 和 `results.xlsx`。

每轮开始前重新验证设备并保存 Excel 快照。Excel 在读取后变化时要求重新读取；运行期间更换设备不会自动切换控制对象，应停止后新建轮次。

## 框架验收示例

可选择 `examples/框架验收.xlsx`：

- “环境检查”：手机 APP 安装检查、眼镜 ADB 在线检查。这两条已在当前真实设备上通过。
- “元素检查”：提供 Appium 页面读取检查，以及当前英文 My 页的 About App 元素检查。My 页检查在前置页面不满足时跳过，不自动导航。

这些是框架验收场景，不代表配对、拍摄、升级等产品功能已经验证通过。真实产品能力尚未确认，待配置型号只允许环境诊断场景，其业务用例会阻塞。

## 输出及状态

每轮输出到 `runs/日期时间_唯一编号/`：

| 文件 | 内容 |
|---|---|
| input.xlsx | 本轮输入快照，供复查和重新导出 |
| manifest.json | 手机、眼镜、名称、产品型号、选中模块、配置及执行状态 |
| results.sqlite | 每条用例的持久化状态和错误原因 |
| results.json / summary.json | 详细结果和模块统计 |
| report.html | 可按编号、模块、状态及原因筛选的中文报告 |
| results.xlsx | 本轮独立 Excel 结果文件 |
| evidence/ | pytest 日志、失败截图及可用会话信息 |

HTML 和数据库区分通过、失败、异常、阻塞、未实现、不适用、跳过、中断和未执行。每个 Sheet 与用例编号联合匹配。

为兼容你提供的模板汇总公式，Excel 回填使用 `Passed / Failed / Block / N/A`。未实现、未执行、跳过和中断的结果格清空，详细原因保留在中文报告中，避免被模板公式误计为已执行。未选模块的历史结果也会在本轮副本中清空，原 Excel 不变。

回填仅修改识别出的用例行的结果列，保留图片、样式、其他列及工作簿组件。汇总页保留原公式并清除缓存，要求 Excel/WPS 打开时重新计算；框架本身不计算 Excel 公式。已有汇总公式的正确性由模板决定。

导出失败时，执行记录仍保留。点击“重新导出历史轮次”，选择含 `manifest.json` 的轮次目录，即可生成新结果文件。强制关闭留下的“执行中”状态在重新导出时按“中断”处理。

## 维护产品、元素和用例

### 产品配置

编辑 `configs/catalog.json`。配置按 `common → products → models` 递归覆盖；字典合并，列表与单值替换。能力使用 `true`（支持）、`false`（不支持）、`null`（未知）。产品型号信息确认后才设置 `configured: true`。

目前已有“G系列”的 G1/G3/G6 配对配置，另保留四款产品占位项及一项“待归类设备”。`Vision Ray 1` 是设备上报名称，其业务产品归属仍需确认。手机和眼镜序列号不写入产品配置。

界面每次启动读取产品配置，修改后重新打开窗口。JSON 是本版实际采用的配置格式。

### 元素维护

元素集中在配置中的 `elements` 字段，使用语义名称，例如 `my.about`。产品或型号可以覆盖同名元素。`studio/android.py` 按配置定位、等待可见状态并检查唯一性。后续页面和业务流程应封装为独立模块，测试不直接散写坐标。

### 注册用例

在对应产品目录（例如 `cases/g_series`）新增 Python pytest 测试，并在该目录的 `registry.json` 注册。根目录 `cases/registry.json` 只维护产品与注册表的映射，`cases/common` 保存公共诊断用例：

```json
{
  "sheet": "G10_我的",
  "id": "My_002",
  "node": "cases/g_series/test_my.py::test_account_navigation",
  "requires": [],
  "diagnostic": false
}
```

`sheet` 和 `id` 必须与 Excel 完全对应。`requires` 填所需能力名称。不能把业务用例标成诊断用例来绕过产品配置。需要实体动作但尚无控制接口的用例可声明 `manual: true`，本版记录为阻塞。

公共 fixture：`context` 读取本轮设备及配置；`adb` 提供显式序列号的设备命令；`android` 建立并清理 Appium 会话。

每条用例在独立进程中执行，默认超时 180 秒，不自动重试。停止或超时会终止该进程并尝试释放已记录的 Appium 会话。pytest 前置、清理异常与断言失败分别统计。

## 当前模板的适配说明

- 读取全部 13 个 Sheet；结果汇总和空的翻译 Sheet 不作为可执行用例模块。
- 自动识别“用例编号”等表头，支持固件更新 Sheet 的第 2 行表头。
- “空间”Sheet 有 1,048,526 行仅填写编号，框架明确显示忽略数量；这些行没有用例内容，不执行、不改写。
- 首次调试发现重复编号和缺失编号；后续重新读取发现文件已更新，当前检查为 687 行用例内容。框架保留校验规则，未自动修改你的编号。
- 仅支持 `.xlsx`。参数化用例应为每个 Excel 编号单独注册，暂不支持多次结果自动合并到一个编号。

## 环境配置及限制

`configs/environment.json` 保存当前电脑 Android SDK 和 Java 路径。换电脑后需要修改路径，并确保 ADB、Appium、UiAutomator2 可用。Appium 服务仅监听本机 `127.0.0.1:4723`，关闭窗口不会停止已运行的服务。

尚未实现：iOS、并行设备、自动硬件工装、业务能力完整配置、自动恢复续跑、多次重试及大批量业务用例。中断后可以重导出，重新执行会新建轮次。

框架验证命令：`python -m unittest discover -s framework_tests -v`。

## OTA 管理后台配置

OTA 上下线脚本使用 `configs/ota_private.json`。该文件只保存在本机，已被 Git 忽略；首次使用可复制 `configs/ota_private.example.json` 后修改：

```json
{
  "base_url": "https://test.dpvr.com",
  "email": "管理后台账号",
  "password": "管理后台密码",
  "ota_path": "/otas"
}
```

后续更换后台环境、账号或 OTA 页面路径时，直接编辑该文件即可。也可通过 `--config 路径` 指定另一份配置，例如：

```powershell
python -m studio.ota online 172 --config configs/ota_private.json
```

任务 ID 是 OTA 操作的唯一目标。默认命令只预览：`python -m studio.ota online 172`；只有附加 `--apply` 才会实际上线或下线，因此执行前先核对任务 ID 和操作类型。
