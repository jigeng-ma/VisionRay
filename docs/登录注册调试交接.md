# 登录注册自动化调试进度（2026-09-17 暂停）

用户因额度要求暂停，调试进程及当前 Appium 会话已停止。没有后台继续跑用例。

## 已完成

- 账号前置按用例独立准备：实际登录探测服务端状态；未注册先注册；需要未注册时存在才注销；密码不符通过邮箱恢复；其他账号不操作。
- 页面导航等待、键盘遮挡处理；取消无条件重注册清理，避免连锁异常和浪费验证码配额。
- 注册/找回/注销发送验证码统一确认响应；明确短时限流等待65秒、最多3次；每日配额、收信超时记录阻塞。重发也已加入限流重试。
- 修正 AUTH_045/046 追加输入被替换的问题，AUTH_051 改为验证真实格式错误拦截。
- 框架检查42项通过，56条用例收集通过。

## 最新已完成结果

整批回归与定向复测合并：52通过、3失败、1阻塞。不是同一轮全量结果。

结果：`outputs/auth-fix-report/report.md`、`outputs/auth-fix-report/results.json`。
原始整批：`runs/20260916_191754_7c0f35d1`。
定向：`runs/20260917_auth_recheck`、`runs/20260917_auth_recheck_final`。

3条保留的APP差异（已复现，不放宽预期）：
- AUTH_016：未注册登录转注册后邮箱为空。
- AUTH_055：找回页点击 Create Account 返回登录页，未进入注册页。
- AUTH_078：重置成功返回登录页邮箱未带入。

## 下次从这里继续

1. AUTH_067：上一完整结果为 BLOCKED（服务端请求过于频繁）。最新重发重试代码已写好；本次复测在限流重试期间按用户要求中断，没有最终结果。证据在 `runs/20260917_auth_recheck_final/AUTH_067`，查看 steps.jsonl、interrupted.txt。优先单独复测，不需要重跑已经通过的所有用例。
2. AUTH_095：此前定向复测已通过。最新队列中安排的最终账号登录/重启确认因暂停未执行；下次067结束后再做。暂停时没有额外操作账号，不能把此前的登录确认当成暂停后的状态确认。
3. 3条APP差异等待APP修复或需求确认后再复测；保留当前严格断言。
4. 原表另40条未实现用例继续沿用此前N列原因，本次未扩展实现。

## 执行方式

工作目录：D:/Jigengma/12_APP_G/02_Auto/VisionRay。
使用 D:/APP/python313/python.exe，PYTHONPATH 为项目根目录及 vendor；设置 GLASSES_RUN_CONTEXT 为 work/auth-generation/context.json；每次指定新的 GLASSES_EVIDENCE_DIR，保留既有证据；PYTEST_DISABLE_PLUGIN_AUTOLOAD=1。
单条节点：cases/g_series/test_auth.py::test_auth[AUTH_067]；随后 AUTH_095。
手机 RFCX916QS9M；确认USB/ADB连接后再运行。同一手机不并发运行。

账号和验证码继续复用 studio/auth.py、studio/mailbox.py 与现有本地配置，不在交接文档复制邮箱授权码。标准APP密码沿用原配置。启动器仍用项目根目录“启动测试工作台.cmd”。

更新报告：PYTHONPATH配置后运行 work/auth-generation/merge_report.py；若使用新结果目录，需要先将该目录加入脚本的结果目录列表。不要覆盖已有结果或把未完成的067标记为通过。
