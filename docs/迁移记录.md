# 工程迁移记录

迁移日期：2026-09-11。

当前工程目录：`D:\Jigengma\12_APP_G\02_Auto\VisionRay`。
原工程目录：`C:\Users\CC\Documents\Codex\2026-09-10\app-ai-1-ui-2-app\outputs\glasses-test-studio`，保留为备份，后续开发使用当前目录。

- 主工程全部 2,126 个文件（127,241,179 字节）复制后逐文件 SHA-256 校验通过，包含源码、依赖、EXE、配置、示例、说明和历史运行结果。
- 相邻的 framework-plan.md、device-inspection.md 已归入 docs。
- 原 work 目录顶层的验证脚本、打包配置和验证记录已归入 work；pyinstaller 构建缓存未迁入。
- work/check_ui.py、work/validate_studio.py 和 work/GlassesStudio.spec 已改为按当前工程位置解析路径。
- 7 项框架单元测试通过；两个验证脚本及打包配置语法检查通过。
- 历史运行记录内的旧绝对路径保留为原始记录。真实设备验证脚本仍使用当时的设备序列号及桌面 Excel 路径，运行前需核对；本次未重新执行设备操作或重新打包 EXE。

## 使用

双击根目录的“启动测试工作台.cmd”。完整说明见根目录“使用说明.md”。

开发验证：`python -m unittest discover -s framework_tests -v`。

安装 PyInstaller 及打包所需依赖后，可从工程根目录执行：
`python -m PyInstaller --distpath bin --workpath work/pyinstaller work/GlassesStudio.spec`
